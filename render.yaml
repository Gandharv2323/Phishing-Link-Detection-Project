services:
  - type: web
    name: hsef-phishing-detection
    env: python
    buildCommand: pip install -r requirements.txt && pip install gunicorn
    startCommand: cd app && gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
